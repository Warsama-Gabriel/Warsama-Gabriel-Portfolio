 
<div class="album text-muted">
	<div class="container">
	  <p><%= link_to 'create new portfolio item', new_portfolio_url if logged_in?(:siteadmin) %></p>
	  <div class="row <%= 'sortable' if logged_in?(:siteadmin) %>">
	    <%= render partial: 'portfolio_item', collection: @portfolio_items %>
	  </div>
	</div>
</div>


<!-- non structured parial use partial => collection -->


